# Biodata Management System - Environment Configuration

# --------------------------------
# LLM Provider Selection
# --------------------------------
# Options: "vertexai", "anthropic", "local" (mock)
LLM_PROVIDER=vertexai

# --------------------------------
# Vertex AI Configuration (Google Cloud)
# --------------------------------
# Get your GCP Project ID from https://console.cloud.google.com/
# Make sure Vertex AI API is enabled in your project
GCP_PROJECT_ID=your-gcp-project-id
GCP_LOCATION=us-central1

# Default Gemini model for OCR
# Available models:
#   - gemini-2.0-flash-001 (fast, good quality)
#   - gemini-2.0-flash-lite-001 (fastest, lightweight)
#   - gemini-2.5-pro-preview-06-05 (best quality, slower)
#   - gemini-2.5-flash-preview-05-20 (balanced)
VERTEX_MODEL=gemini-2.0-flash-001

# --------------------------------
# Anthropic Configuration (Alternative)
# --------------------------------
# Get your API key from https://console.anthropic.com/
# ANTHROPIC_API_KEY=your_anthropic_api_key_here

# --------------------------------
# OpenAI Configuration (Not yet implemented)
# --------------------------------
# OPENAI_API_KEY=your_openai_api_key_here

# --------------------------------
# Storage Settings
# --------------------------------
# Storage Type: "local" or "cloud"
STORAGE_TYPE=local

# For cloud storage (future implementation)
# GCS_BUCKET_NAME=your-bucket-name

# --------------------------------
# App Settings
# --------------------------------
DEBUG=true

# --------------------------------
# Rate Limiting & Queue Settings
# --------------------------------
# Maximum concurrent OCR tasks (affects memory usage)
QUEUE_CONCURRENCY=5

# API requests per minute (stay under provider limits)
# Vertex AI Tier 1: 60 RPM, Anthropic Tier 1: 60 RPM
REQUESTS_PER_MINUTE=50

# Maximum burst requests allowed
BURST_CAPACITY=10

# Retry settings for transient failures (429, 503, etc.)
MAX_RETRIES=3
RETRY_BASE_DELAY=2.0
RETRY_MAX_DELAY=60.0

# Batch processing settings
BATCH_CHUNK_SIZE=10
MAX_BULK_FILES=200
